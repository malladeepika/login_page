apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx
  
spec:
  # modify replicas according to your case
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: ngnix
    spec:
      containers:
      - name: c1
        image: nginx
        ports:
        - containerPort: 80
